Hi! JMK here, artillery and skeleton key maintainer.  

by default, we follow IWYU. this means that I have only set up concurrent hash for use with UE. You'll want to take a look
at timer.hpp and the way we use concurrent_map.hpp as well as concurrent_hash_table if you want more from seq.